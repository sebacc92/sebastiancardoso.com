import { component$ } from "@builder.io/qwik";

export default component$(() => {
    return (
        <div class="flex-grow m-[0_auto] max-w-4xl px-6 sm:px-4 ">
            <main class="flex justify-center mt-4">
                <h1>Projects en construcción</h1>
            </main>
        </div>
    );
});
